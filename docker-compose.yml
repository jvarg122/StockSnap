services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: stock_snap
      POSTGRES_USER: stock_snap
      POSTGRES_PASSWORD: devpassword
    ports:
      - "5434:5432"
    volumes:
      - stock_snap_pgdata:/var/lib/postgresql/data

volumes:
  stock_snap_pgdata:
